# CI
# Continuous build + unit-test check on every push and pull request to the
# main development branches. Proves the repo is self-contained (a clean clone
# builds with no local-only dependencies) and runs the fast unit tests.
#
# This complements release.yml (which builds + publishes the production DLL on
# a v* tag). CI builds the Dev DLL and does not publish anything.
name: CI

on:
  push:
    branches: [develop, main]
  pull_request:
    branches: [develop, main]

# Avoid redundant runs when pushing to a branch that has an open PR.
concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

# Least-privilege GITHUB_TOKEN. CI only checks out, builds, and tests — it
# publishes nothing, so read-only access to the repo contents is sufficient.
# (release.yml sets contents: write because it creates the GitHub Release.)
permissions:
  contents: read

jobs:
  build-and-test:
    # Pinned to windows-2022 (Visual Studio 2022). windows-latest moved to the
    # VS18 image, where the "Visual Studio 17 2022" CMake generator no longer
    # resolves ("could not find any instance of Visual Studio"). Pin keeps the
    # 32-bit VS2022 toolchain Zorro requires. Revisit when adding VS18 support.
    runs-on: windows-2022

    # Disable vcpkg GHA binary caching (see release.yml for rationale).
    env:
      VCPKG_BINARY_SOURCES: clear

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up MSBuild
        uses: microsoft/setup-msbuild@v2

      - name: Set up Developer Command Prompt (Win32)
        uses: ilammy/msvc-dev-cmd@v1
        with:
          arch: x86

      - name: Set up vcpkg
        uses: lukka/run-vcpkg@v11
        with:
          # Full 40-char commit SHA of the 2026.01.16 vcpkg release tag. Keep it
          # in step with builtin-baseline in vcpkg.json; the two must resolve the
          # same port versions. A vcpkg revision pins exact MSYS2 package versions
          # for the build tools it acquires, and the MSYS2 mirrors carry only
          # current versions — so this pin moves forward, never back.
          vcpkgGitCommitId: '66c0373dc7fca549e5803087b9487edfe3aca0a1'
          runVcpkgInstall: false

      # The Zorro SDK headers (include/trading.h, include/variables.h) are
      # proprietary (c) oP group and are NOT committed to this public repo
      # (include/ is gitignored). The API layer needs them, so CI extracts just
      # those two headers from the official Zorro beta distribution. No secret
      # required. The full installer is never run — we only unzip two headers.
      - name: Cache Zorro SDK zip
        id: zorro-cache
        uses: actions/cache@v4
        with:
          path: zorro-sdk.zip
          key: zorro-beta-sdk-zip-v1
      - name: Download Zorro beta (for SDK headers)
        if: steps.zorro-cache.outputs.cache-hit != 'true'
        shell: bash
        run: curl -fsSL -o zorro-sdk.zip https://opserver.de/down/ZorroBeta.zip
      - name: Stage Zorro SDK headers
        shell: bash
        run: |
          mkdir -p include
          unzip -o -j zorro-sdk.zip "ZorroBeta/include/trading.h" "ZorroBeta/include/variables.h" -d include
          test -f include/trading.h && test -f include/variables.h
          echo "Staged trading.h + variables.h into include/"

      - name: Configure CMake (Dev, Win32)
        shell: cmd
        run: |
          mkdir build_ci
          cd build_ci
          cmake .. -G "Visual Studio 17 2022" -A Win32 -DDEV_BUILD=ON ^
            -DCMAKE_TOOLCHAIN_FILE=%VCPKG_ROOT%/scripts/buildsystems/vcpkg.cmake ^
            -DVCPKG_TARGET_TRIPLET=x86-windows-static

      - name: Build plugin DLL
        shell: cmd
        run: cmake --build build_ci --config Release --target Hyperliquid

      - name: Verify DLL produced
        shell: bash
        run: |
          if [ ! -f "build_ci/Release/Hyperliquid_Dev.dll" ]; then
            echo "::error::Hyperliquid_Dev.dll not produced"
            exit 1
          fi
          echo "DLL size: $(stat -c%s build_ci/Release/Hyperliquid_Dev.dll) bytes"

      - name: Build + run unit tests
        shell: cmd
        run: |
          cmake --build build_ci --config Release --target test_ws_price_cache
          build_ci\Release\test_ws_price_cache.exe
